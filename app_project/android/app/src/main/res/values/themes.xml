<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Framework Material dark theme (no AndroidX dependency); the dark window
         background matches the app so there's no white flash before it loads. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/appbg</item>
        <item name="android:statusBarColor">@color/appbg</item>
        <item name="android:navigationBarColor">@color/appbg</item>
        <item name="android:colorPrimary">@color/brand</item>
    </style>
</resources>
